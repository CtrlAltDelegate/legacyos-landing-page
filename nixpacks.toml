[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd server && npm ci --include=dev && npx prisma generate && npm run build"]

[start]
cmd = "cd server && npx prisma db push --accept-data-loss && node dist/index.js"
